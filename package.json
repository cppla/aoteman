{
  "name": "aoteman-galaxy-companion",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "description": "银河小伙伴 · 奥特曼电子宠物",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "dev": "docker compose up --build",
    "test:browser": "node tests/browser.mjs",
    "test:api": "python3 -m unittest discover -s tests -p test_api.py -v",
    "test:sync-browser": "node tests/server-save-browser.mjs",
    "test:battle-save": "node tests/battle-save-browser.mjs"
  },
  "engines": { "node": ">=20" },
  "devDependencies": { "playwright": "1.62.1" }
}
